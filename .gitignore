# node
node_modules/

# env & secrets (never commit these)
.env
.env.*
*.pem
*.p12
*.key
*.crt
*.pfx
*.der
*.jks
*.keystore
*.kdb

# if you keep JSON secrets, put them under /secrets and ignore that folder:
secrets/**/*.json
